## Overviwe
```
In this example i have learned about Modle.how to create database tables from models

```

### from django.db import models
```

- In Django, models is a module inside django.db that contains classes and methods to define database tables

- Model is a base class inside models that is used to define a database table in Django

- CharField and other fields are classes defined in the models module inside Django.
```